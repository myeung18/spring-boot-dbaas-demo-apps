---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: dbaas-spring-postgresql-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dbaas-spring-postgresql-app
  template:
    metadata:
      labels:
        app: dbaas-spring-postgresql-app
        deploymentconfig: dbaas-spring-postgresql-app
    spec:
      containers:
        - name: dbaas-spring-postgresql-app
          image: quay.io/myeung/dbaas-spring-postgresql-demo:v0.0.1
          ports:
            - containerPort: 8080
              protocol: TCP
          imagePullPolicy: Always
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: dbaas-spring-postgresql-app
  name: dbaas-spring-postgresql-app
spec:
  ports:
    - name: 8080-tcp
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: dbaas-spring-postgresql-app
    deploymentconfig: dbaas-spring-postgresql-app
  type: ClusterIP
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: dbaas-spring-postgresql-app
  name: dbaas-spring-postgresql-app
spec:
  to:
    kind: Service
    name: dbaas-spring-postgresql-app
